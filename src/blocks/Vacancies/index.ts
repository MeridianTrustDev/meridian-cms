import { Block } from 'payload/types'

export const Vacancies: Block = {
  slug: 'vacancies',
  fields: [
    {
      name: 'myNewTermID',
      label: 'MyNewTerm ID',
      type: 'text',
    },
  ],
}
